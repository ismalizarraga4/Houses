{
  "name": "houses",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon index.js",
    "start": "node index.js",
    "devt": "set NODE_OPTIONS=--experimental-vm-modules && jest && nodemon index.js",
    "devtU": "NODE_OPTIONS=--experimental-vm-modules && jest && nodemon index.js",
    "test": "set NODE_OPTIONS=--experimental-vm-modules && jest",
    "testU": "NODE_OPTIONS=--experimental-vm-modules && jest"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "jest": "^28.1.2",
    "nodemon": "^2.0.18",
    "supertest": "^6.2.4"
  }
}
